<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.phone48.mapper.BoardMapper">
	<select id="selectBoard" parameterType="int" resultType="board">
		select * from (select b.*, ceil(rownum/5)  as pageno from 
		(select * from board order by bno desc) b)
		where pageno = #{pageno}
	</select>
	<select id="selectBoardNo" resultType="int">
		select board_no_seq.nextval from dual
	</select>
	<insert id="insertBoard" parameterType="board">
		insert into board(bno, id, nickname, title, content, price, stag)
		values(#{bno},#{id},#{title},#{nickname},#{content},#{price},#{pick})
	</insert>
	<insert id="insertFileInfo" parameterType="file">
		insert into board_file_list values(file_no_seq.nextval,#{bno},#{nickname},#{path},#{originFileName})
	</insert>
	<select id="selectBoardCount" resultType="int">
		select count(*) from board
	</select>
	<delete id="deleteBoard" parameterType="int">
		delete from board where bno = #{bno}
	</delete>
	<select id="selectBoardContent" parameterType="int" resultType="board">
		select bno, id, nickname, title, price, pick from board
	</select>
	<update id="updateBoard" parameterType="map">
		update board set title=#{title},content=#{content} where bno = #{bno}
	</update>
	<update id="addBoardCount" parameterType="int">
		update board set bno = bno + 1 
	</update>	
 	<!--  리뷰 갯수 -->
     <!-- <select id="boardCount" resultType="board">
        SELECT
            COUNT(*)
        FROM REVIEW
    </select>
    
    리뷰 리스트
    <select id="reviewList" resultType="review">
        SELECT
            rno,id,nickname,review_content,review_point
        FROM REVIEW
    </select>
    
    리뷰 상세 페이지
    <select id="reviewDetail" parameterType="int" resultType="review">
        SELECT
            *
        FROM REVIEW
        WHERE
            RNO = #{rno}
    </select>
	
	리뷰 작성
    <insert id="reviewInsert" parameterType="map">
        INSERT INTO
        BOARD (RNO,ID,NICKNAME,REVIEW_CONTENT,REVIEW_POINT)
        VALUES(#{rno},#{id},#{nickname},#{review_content},#{review_point}) 
    </insert>
    
    페이징 
    <select id="boardList" resultType="hashmap" parameterType="hashmap">
        <![CDATA[
            SELECT * 
            FROM REVIEW
            ORDER BY RNO DESC
            LIMIT #{pageStart}, #{perPageNum}
        ]]>
		</select>
		<select id="boardListCnt" resultType="int">
		        SELECT count(*)
		        FROM REVIEW
		</select>	 -->
</mapper>